package br.ufg.inf.es.construcao.algoritmo40;

import org.junit.Test;
import static org.junit.Assert.*;

public class AtendeSomaFatDigitosTest {

    /**
     * Classe não pode ser testada pois utiliza algoritmos externos já
     * implementados, e que não aceitam valores que são repassados causando
     * invalidade de testes.
     */
    @Test
    public void testAtendeSomaFatDigitos() {
        //assertTrue(AtendeSomaFatDigitos.atendeSomaFatDigitos(3));
    }
}
